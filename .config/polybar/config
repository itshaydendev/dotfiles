[colors]
background = #000000
background-active = #9112ED
foreground = #ffffff

[bar/top]
monitor = ${env:MONITOR:}
width = 100%
height = 32
radius = 0.0
fixed-center = true
wm-restack = bspwm

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 0
line-color = #f00

padding-left = 0
padding-right = 0

module-margin-left = 0
module-margin-right = 3

font-0 = monospace:style=regular:antialias=true:pixelsize=12;2

modules-left = bspwm
modules-center = time
tray-position = right
tray-padding = 0
tray-background = ${colors.background-active}

[module/bspwm]
type = internal/bspwm

label-focused = %name%
label-focused-background = ${colors.background-active}
label-focused-padding = 1

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%!
label-urgent-background = ${colors.foreground}
label-urgent-foreground = ${colors.background}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${colors.foreground}
label-empty-padding = 1
pin-workspaces = false

ws-icon-0 = web

[module/mpd]
type = internal/mpd
format-online = <icon-prev>  <label-song>  <icon-next>

icon-prev = «
icon-next = »

label-song-maxlen = 30
label-song-ellipsis = true

[module/xbacklight]
type = internal/xbacklight

format = <label> <bar>
label = BL

bar-width = 10
bar-indicator = |
bar-indicator-foreground = #fff
bar-indicator-font = 2
bar-fill = ─
bar-fill-font = 2
bar-fill-foreground = #9f78e1
bar-empty = ─
bar-empty-font = 2
bar-empty-foreground = ${colors.foreground-alt}

[module/date]
type = internal/date
date = %A, %e %B %Y
label = %date%

[module/time]
type = internal/date
time = %H:%M
label = %time%

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 99

label-charging = %percentage%%
label-discharging = %percentage%%
format-charging = <label-charging>
format-discharging = <label-discharging>
format-full = 100

[settings]
screenchange-reload = true

[global/wm]
margin-top = 0
margin-bottom = 0
